allow_username_mismatch = true;
sign_authenticated = true;
sign_local = true;
use_domain = "header";
use_esld = false;
try_fallback = true;

domain {
  example.test {
    selector = "mail";
    path = "/run/rspamd/dkim/example.test.mail.key";
  }
}
