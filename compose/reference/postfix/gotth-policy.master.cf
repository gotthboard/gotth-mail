gotth_policy unix - n n - - pipe
  flags=q user=nobody null_sender= argv=/usr/local/bin/gotth-mail-postfix-gate deliver --queue-id=${queue_id} --authenticated-mailbox=${sasl_username} --original-recipient=${original_recipient} --recipient=${recipient}
