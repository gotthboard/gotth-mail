# GOTTH Mail 1.0 Identity + Provisioning

Historical workflow ID retained below; this is not product v2.

ID: `v2.identity-provisioning`

State: `in_progress`

Canonical state lives in `workflow.toml`. This folder holds scoped evidence, review notes, and postmortems only.
