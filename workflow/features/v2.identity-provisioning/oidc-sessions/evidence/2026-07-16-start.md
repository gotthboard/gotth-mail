# v2 OIDC sessions start evidence

Date/time: 2026-07-16 10:50 CDT

Commit: current commit; hash assigned by Git after commit

## Scope

Started `v2.identity-provisioning` after v1 admission PR #2 merged into `main` at `9f055a6`.

Active child:

- `v2.identity-provisioning.oidc-sessions`

Required implementation surface:

- OIDC authorization-code login;
- single-use state;
- nonce validation;
- redirect URI validation;
- issuer/audience/azp/exp/iat/nbf validation;
- signed-token validation through configured issuer/JWKS;
- no unsigned-claim fallback;
- no token disclosure;
- session creation/expiry/revocation behavior.

## Verification for this start marker

```text
git diff --check -- .
go test ./...
```
