# Authentik role mapping and full permission simulator coverage

ID: `v2.identity-provisioning.authentik-roles-authz`

State: `done`

Canonical state lives in `workflow.toml`. This folder holds scoped evidence, review notes, and postmortems only.
