# GOTTH identity library compatibility

Pins and exercises the public `gotth-oidc` and `gotth-scim` consumer APIs.
Runtime cutover remains part of the identity workstream and cannot be inferred
from this compatibility proof.
