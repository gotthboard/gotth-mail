# v5 Notifications

ID: `v5.notifications`

State: `in_progress`

All three repository implementation children are done. The historical v5 root
remains open only because its declared `v3.ops-import-admin` root dependency is
still open; no live Telegram credential, webhook registration, deployment, or
production signing key was created here.

Canonical state lives in `workflow.toml`. This folder holds scoped evidence, review notes, and postmortems only.
